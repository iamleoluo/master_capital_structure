# 建置當日實測版本(Python 3.9)
requests>=2.28
numpy>=1.22
matplotlib>=3.5

# 選用:pandas 不是必要相依,核心計算與繪圖都不用它
# yfinance 刻意**不列入** —— 0.2.36 在本機無法取得 MSTR(No timezone found),
# fetch.py 改為直連 Yahoo chart 端點,只需要 requests。
